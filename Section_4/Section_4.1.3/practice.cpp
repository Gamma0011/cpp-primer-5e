#include <iostream>

using std::cout;
using std::cin;
using std::endl;

int main()
{
    /* e43 - I see binary operators being left undefined as an acceptable trade-off.
        Ideally, coders should make sure that the binary operands are computed as expected for most complicated computation.
        The versatility of being able to use the basic, built-in order of evaluation does save compute time,
        so long as the end output is expected and consistent.
    */

    return 0;
}