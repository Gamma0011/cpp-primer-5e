#include <iostream>
#include <cctype>


// isalnum(c)   = true if c is a letter or a digit;
// isalpha(c)   = true if c is a letter;
// iscntrl(c)   = is a control character;
// isdigit(c)   = true if c is a digit;
// isgraph(c)   = true if c is not a space but is printable;
// islower(c)   = true if c is a lowercase character;
// isprint(c)   = true if c is printable (ie space or character that has a visual representation);
// ispunct(c)   = true if c is a punctuation character;
// isspace(c)   = true if c is whitespace;
// isupper(c)   = true if c is uppercase character;
// isxdigit(c)  = true if c is a hexadecimaql digit;
// tolower(c)   = if c is uppercase, returns lowercase equivalent; otherwise c unchanged;
// toupper(c)   = if c is lowercase, returns uppercase equivalent; otherwise c unchanged;

int main()
{

    return 0;
}