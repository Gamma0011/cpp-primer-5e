Reference: Only refers to an object. Must be initialized at declaration, it cannot be changed. Does not take up any memory.

Pointer: Can be initialized whenever, but recommended to always initialize to at least nullptr or 0;
        Pointers and their references can be changed to point to different sections of memory and different stored values
        Takes up memory